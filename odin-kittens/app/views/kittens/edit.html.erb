<h1>Edit the kitten</h1>

<%= render "form" %>

<div class="action-btn"><%= button_to "Delete", kitten_path(@kitten), :method => :delete, data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></div>
